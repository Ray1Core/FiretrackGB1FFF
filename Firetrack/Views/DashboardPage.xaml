<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Firetrack.Views.DashboardPage"
             Title="Dashboard"
             Shell.TabBarIsVisible="True">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            <!-- Header -->
            <VerticalStackLayout Spacing="6" HorizontalOptions="Center">
                <Label Text="🔥 Fire Track"
                       FontSize="32"
                       FontAttributes="Bold"
                       TextColor="{StaticResource PrimaryColor}"
                       HorizontalOptions="Center" />
                <Label Text="{Binding WelcomeMessage}"
                       FontSize="18"
                       TextColor="{StaticResource TextPrimaryColor}"
                       HorizontalOptions="Center" />
                <Label Text="{Binding UserRole, StringFormat='Role: {0}'}"
                       FontSize="14"
                       TextColor="{StaticResource TextSecondaryColor}"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- Equipment List Card -->
            <Border Style="{StaticResource CardStyle}" Padding="0">
                <VerticalStackLayout>
                    <Label Text="📦 Your Assigned Equipment"
                           FontAttributes="Bold"
                           FontSize="16"
                           TextColor="{StaticResource PrimaryColor}"
                           Margin="15,10,15,5" />
                    <CollectionView ItemsSource="{Binding MyEquipment}" HeightRequest="200">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*, Auto, Auto, Auto" Padding="10,6">
                                    <Label Text="{Binding Name}"
                                           Grid.Column="0"
                                           FontSize="14"
                                           TextColor="{StaticResource TextPrimaryColor}" />

                                    <!-- Status Label with Tap Gesture -->
                                    <Label Grid.Column="1"
                                           Text="{Binding Status}"
                                           FontSize="12"
                                           Padding="6,2"
                                           Margin="5,0"
                                           HorizontalTextAlignment="Center"
                                           TextColor="White"
                                           BackgroundColor="{Binding Status, Converter={StaticResource StatusToColorConverter}}">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ShowEquipmentDetailsCommand}"
                                                                  CommandParameter="{Binding .}" />
                                        </Label.GestureRecognizers>
                                    </Label>

                                    <!-- Return Button -->
                                    <Button Grid.Column="2"
                                            Text="↩️ Return"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ReturnEquipmentCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{StaticResource WarningColor}"
                                            TextColor="White"
                                            FontSize="11"
                                            CornerRadius="6"
                                            Padding="8,4"
                                            HeightRequest="30" />

                                    <!-- Report Damage Button -->
                                    <Button Grid.Column="3"
                                            Text="⚠️ Report"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ReportDamageCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{StaticResource DangerColor}"
                                            TextColor="White"
                                            FontSize="11"
                                            CornerRadius="6"
                                            Padding="8,4"
                                            HeightRequest="30" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <!-- Common Buttons (visible to all users) -->
            <Button Text="📋 Request Equipment"
                    Command="{Binding GoToRequestEquipmentCommand}"
                    Style="{StaticResource PrimaryButton}" />

            <Button Text="👤 Profile"
                    Command="{Binding GoToProfileCommand}"
                    Style="{StaticResource PrimaryButton}" />

            <!-- Action Buttons Grid (Admin only) -->
            <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto" ColumnSpacing="12" RowSpacing="12">
                <!-- Row 0: Scan QR (common) -->
                <Button Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                        Text="📷 Scan QR"
                        Command="{Binding GoToScannerCommand}"
                        Style="{StaticResource PrimaryButton}" />

                <!-- Row 1: Admin Only -->
                <Button Grid.Row="1" Grid.Column="0"
                        Text="📱 Generate QR"
                        Command="{Binding GoToGenerateCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />
                <Button Grid.Row="1" Grid.Column="1"
                        Text="🔄 Transfer"
                        Command="{Binding GoToTransferCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />

                <!-- Row 2: Admin Only -->
                <Button Grid.Row="2" Grid.Column="0"
                        Text="👤 Add User"
                        Command="{Binding GoToAddUserCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />
                <Button Grid.Row="2" Grid.Column="1"
                        Text="📋 Clearance"
                        Command="{Binding GoToClearanceCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />

                <!-- Row 3: Admin Only -->
                <Button Grid.Row="3" Grid.Column="0"
                        Text="👥 User Management"
                        Command="{Binding GoToUserManagementCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />
                <Button Grid.Row="3" Grid.Column="1"
                        Text="📋 Inventory"
                        Command="{Binding GoToInventoryCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />

                <!-- Row 4: Admin Only (NEW) -->
                <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                        Text="📋 Pending Requests"
                        Command="{Binding GoToPendingRequestsCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsVisible="{Binding IsAdmin}" />

                <!-- Row 5: Logout (common) -->
                <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                        Text="🚪 Logout"
                        Command="{Binding LogoutCommand}"
                        Style="{StaticResource DangerButton}" />
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>